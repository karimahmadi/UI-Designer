// import { selectAuthenticatorDomain } from '../selectors';

describe('selectAuthenticatorDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
