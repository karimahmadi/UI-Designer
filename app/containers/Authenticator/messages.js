/*
 * Authenticator Messages
 *
 * This contains all the text for the Authenticator container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Authenticator';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Authenticator container!',
  },
});
