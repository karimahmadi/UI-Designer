// import { selectGlobalServicesDomain } from '../selectors';

describe('selectGlobalServicesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
