/**
 * Test sagas
 */

/* eslint-disable redux-saga/yield-effects */
// import { take, call, put, select } from 'redux-saga/effects';
// import {{ camelCase crudModulePrefix }}ViewSaga from '../saga';

// const generator = {{ camelCase crudModulePrefix }}ViewSaga();

describe('{{ camelCase crudModulePrefix }}ViewSaga Saga', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
