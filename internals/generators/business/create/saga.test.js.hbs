/**
 * Test sagas
 */

/* eslint-disable redux-saga/yield-effects */
// import { take, call, put, select } from 'redux-saga/effects';
// import {{ camelCase crudModulePrefix }}CreateSaga from '../saga';

// const generator = {{ camelCase crudModulePrefix }}CreateSaga();

describe('{{ camelCase crudModulePrefix }}CreateSaga Saga', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
