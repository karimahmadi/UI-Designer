/**
 * Test sagas
 */

/* eslint-disable redux-saga/yield-effects */
// import { take, call, put, select } from 'redux-saga/effects';
// import {{ camelCase crudModulePrefix }}EditSaga from '../saga';

// const generator = {{ camelCase crudModulePrefix }}EditSaga();

describe('{{ camelCase crudModulePrefix }}EditSaga Saga', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
